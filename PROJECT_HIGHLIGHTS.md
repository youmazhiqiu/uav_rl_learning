# 基于 Q-learning 的无人机强化学习控制 Baseline

## 一句话简介

使用 Python 搭建单 UAV 强化学习控制 baseline，完成二维环境建模、Q-learning 实现、可重复对比实验、独立策略评估和结果可视化，跑通从算法到实验结论的完整流程。

## 项目工作

- 搭建固定起点与目标的二维离散 UAV 导航环境，设计状态、四方向动作、距离奖励和终止条件。
- 实现基于 Q 表的 Q-learning 与 ε-greedy 策略，将训练和评估分开，评估时关闭随机探索且不更新 Q 表。
- 引入随机种子并设计 100 回合与 500 回合控制变量实验，验证独立 100 回合运行与 500 回合运行的前 100 回合完全一致。
- 自动记录回报、步数、是否成功和最终距离，计算成功率，并保存 Q 表和关闭探索后的评估轨迹。
- 使用 Matplotlib 绘制训练曲线，并制作两组策略的轨迹对比 GIF。
- 使用 Git / GitHub 管理项目版本，在 WSL 中完成开发，并以 CSV、Q-table、PNG 和 GIF 归档实验产物。

## 实验结果

- 训练 100 回合后，关闭随机探索的策略运行 100 步仍未到达目标，并进入周期振荡。
- 训练 500 回合后，同样的评估策略用 10 步到达 `(5,5)`；10 步是当前四方向单步移动模型下的最短路径。
- 在 500 回合训练记录中（仍包含 ε-greedy 探索），前 50 回合与后 50 回合相比：平均回报由 `-582.19` 提升至 `45.03`，平均步数由 `64.92` 降至 `15.00`，成功率由 `58%` 提升至 `100%`。

## 问题分析

早期版本同样训练 100 回合，但没有固定随机种子；ε-greedy 的随机探索历史不同，最终形成了另一种往返振荡的失败策略。固定 `seed=0` 后，100 回合独立运行仍失败，而采用相同设置的 500 回合独立运行成功；两者的前 100 回合记录完全一致。这个过程不仅验证了算法实现，也通过随机种子、控制变量、训练指标和轨迹，分析了训练量和随机探索历史与失败策略之间的关系。该对比说明，评估策略时还需要保留训练过程与轨迹证据，才能解释最终结果。

## 技术栈

Python / Reinforcement Learning / Q-learning / Matplotlib / CSV / Git / GitHub / WSL

## 项目价值

项目完成了算法理解、代码实现、实验设计、策略评估、数据分析和可视化的闭环，建立了可重复、可观测的强化学习实验流程，也体现了将算法概念落地为可运行工程并用数据解释策略行为的能力。当前成果可作为后续 multi-seed、随机起终点、DQN、UAV 动力学、多 UAV、CTDE 及 GNN/GAT 研究的基础 baseline；这些扩展尚未实现。
